@using System;
@using System.Collections.Generic;
@using System.Linq;
@using Sandbox;
@using Sandbox.UI;
@using Sandbox.UI.GameMenu;
@inherits Panel
@attribute [StyleSheet]

@namespace Rhythm4K

<root>
    <PackageList ShowFilters="@true" Query="ref:carsonk.rhythm4k type:addon sort:trending" OnSelected="@OnPackageSelected" Take=@(50)></PackageList>
    <a class="button" href="/songs"> &lt; Back</span>
</root>

@code
{
    void OnPackageSelected(Package package)
    {
        if(Ancestors.FirstOrDefault(x => x is Rhythm4KSingleMenu) is Rhythm4KSingleMenu menu)
        {
            if(menu.Children.FirstOrDefault(x => x is SingleplayerPage) is SingleplayerPage sp)
            {
                sp.MountAddon(package);
            }
        }

        this.Navigate("/songs");
    }
}